apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airbus-persistent-volume-claim
spec:
  storageClassName: gcp-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 256Mi
