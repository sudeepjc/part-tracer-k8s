apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: network-persistent-volume-claim
spec:
  storageClassName: gcp-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
