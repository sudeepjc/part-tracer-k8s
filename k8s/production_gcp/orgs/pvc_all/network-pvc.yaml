apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: network-persistent-volume-claim
spec:
  storageClassName: gcp-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
