apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: regulator-persistent-volume-claim
spec:
  storageClassName: gcp-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 256Mi
