apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: general-persistent-volume-claim
spec:
  storageClassName: gcp-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
